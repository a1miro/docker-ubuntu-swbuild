services:
    swbuild-ubuntu-18.04:
        build:
            context: .
            dockerfile: Dockerfile
            args:
                uid: ${uid}
                gid: ${gid}
                username: ${username}
        image: swbuild-ubuntu-18.04
        container_name: swbuild-ubuntu
        restart: on-failure
        environment:
            - ALLOW_EMPTY_PASSWORD=yes
            - TZ=Europe/London
            - DEBIAN_FRONTEND=noninteractive
            - VCPKG_ROOT=/opt/vcpkg
        volumes:
            - homedir:/home
            - appsdir:/opt/apps
        hostname: swbuild-ubuntu-18.04
        tty: true
volumes:
    homedir:
    appsdir:
